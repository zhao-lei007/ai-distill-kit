
---

## 上级知识库（由 ai-distill-kit 追加）

本项目属于岗位目录 `<岗位目录路径>`。开工前：

- 涉及 <分类1> 的任务，先读 `../业务知识/<分类1>.md` 的对应条目，不要一次全读。
- 涉及 <分类2> 的任务，先读 `../业务知识/<分类2>.md` 的对应条目。
- 通用规矩见 `../AGENTS.md`。

发现知识与现场事实不符，先告诉我，不要自行修改上级知识文件。

## 会话末尾自记（本工具读不到会话记录时启用）

每次会话结束前，或我说「收工」时：把本次会话里我**手打**的、包含业务判断或规则的原话，逐条追加到 `_候选/本项目候选.md`，格式：

```
- [YYYY-MM-DD] 原话（脱敏后）
```

只记我说的，不记你说的；只记判断和规则，不记任务指令。每周蒸馏从这个文件读。
