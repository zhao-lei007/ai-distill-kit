---
name: ai-distill-kit
description: 在用户电脑上搭建「岗位目录 + 项目目录」两层结构，从本工具的会话记录里提取用户手打的业务判断，蒸馏成岗位知识库，并建立每周例行。用户提到「知识蒸馏」「沉淀知识」「搭岗位目录」「蒸馏自己」时使用。
---

# ai-distill-kit：知识蒸馏落地步骤

你是用户的 AI 助手。用户把这份文件交给你，是要你在他的电脑上把知识沉淀的结构搭起来，并完成第一次蒸馏。全程在本机完成，不上传任何内容。

按下面七个阶段顺序执行。每个阶段结束用一两句话向用户汇报，涉及移动文件、写入已有文件的动作，先说明再等用户同意。

## 通用护栏（全程有效）

1. 不移动、不删除任何已有文件或目录，除非用户在对话里明确同意该次操作。
2. 不修改 `.git/` 目录下任何内容。
3. 对已有的 `AGENTS.md` / `CLAUDE.md`，只在末尾追加，不改写原文。
4. 写入知识文件前脱敏：密码、令牌、API Key、内网地址、数据库连接串、手机号、邮箱一律替换为 `<已脱敏>`。
5. 机器只能给知识条目标「待复核」或「阶段性」，不能标「已失效」。失效只有用户能宣布。
6. 判断不了的事情问用户，不要猜。

## 阶段 0：确认环境

1. 判断你自己是哪种工具，依据是能读到哪些目录：
   - 有 `~/.codex/` → Codex
   - 有 `~/.dsh/` → DeepSeek Harness
   - 有 `~/.workbuddy/` → WorkBuddy
   - 有 `~/.claude/` → Claude Code
   多个同时存在时，以当前运行的为准；不确定就问用户。
2. 读本仓库 `tools/README.md` 里对应工具的那一节，记住：会话记录在哪、规矩文件读取范围、有没有兜底。
3. 记录当前工作目录。如果当前目录不像一个项目文件夹（没有 `.git` 也没有 `AGENTS.md`），提醒用户：本方案的前提是至少有一个正在用的项目文件夹，并问他项目在哪。
4. 向用户说明你接下来会做的七件事，以及你不会做的事（不上传、不删、不擅自挪）。

## 阶段 1：盘点项目文件夹

目标：列出用户电脑上正在用 AI 助手干活的项目文件夹。

1. 先问用户：「你目前用 AI 助手干活的项目文件夹有哪些？给我路径，或者让我从会话记录里统计。」
2. 如果用户让你统计，按 `tools/README.md` 的方法读本工具的会话记录，统计每个工作目录出现的会话数，取最近 90 天内出现过的目录。Codex 可以直接运行 `tools/extract_codex.py --list-projects`。
3. 剔除明显不是项目的目录：用户家目录本身、`Downloads`、`Desktop`、临时目录。
4. 对剩下的每个目录检查：有没有 `.git`、有没有 `AGENTS.md` 或 `CLAUDE.md`、最近一次会话日期。
5. 用表格列给用户，让他勾选哪些是要纳入蒸馏的项目。没被勾选的不再处理。

## 阶段 2：确定岗位目录

目标：在项目目录之上定一个岗位目录，作为知识库的家。

判断顺序：

1. **优先就地升级**。如果用户勾选的项目都在同一个父目录下，且这个父目录不是家目录、不是 `Downloads` 这类杂物目录，就把这个父目录当作岗位目录。什么都不用挪。
2. **其次新建**。如果项目分散在不同位置，建议用户新建一个岗位目录（名字让用户定，例如 `~/我的岗位/`），然后只把他点名的项目挪进去。
3. **挪动前必须做三件事**，缺一不可：
   - 告诉用户：AI 工具的会话记录按路径索引，挪动后旧会话在工具里可能找不到、或者需要重新打开。
   - 先对要挪的项目完成阶段 5 的考古提取，把旧路径下的知识先捞出来。
   - 在岗位目录的 `业务知识/_迁移记录.md` 里登记「旧路径 → 新路径 → 挪动日期」。
4. 用户不愿意挪的项目，留在原地，岗位目录仍然可以引用它：在 `业务知识/_迁移记录.md` 里登记「外部项目：路径」即可。

得到用户确认后再进入阶段 3。

## 阶段 3：搭骨架

1. 问用户：「你的工作大致分几类？给我 2 到 4 个分类名。」举例：报表处理、需求评审、数据分析、AI 相关。分类名就是正式知识文件名。
2. 在岗位目录下创建：
   ```
   AGENTS.md                      ← 用 templates/岗位-AGENTS.md，替换分类名
   业务知识/
     <分类1>.md                   ← 用 templates/业务知识-分类文件.md
     <分类2>.md
     _候选/观察池.md               ← 用 templates/观察池.md
     CHANGELOG.md                 ← 用 templates/CHANGELOG.md
   ```
3. 如果岗位目录已有 `AGENTS.md`，不覆盖，把模板内容追加到末尾。
4. 把本仓库的 `prompts/weekly-distill.md` 复制到 `业务知识/_候选/weekly-distill.md`，方便以后离线使用。
5. 是否给岗位目录建 Git 仓库：问用户。建议建，用于追踪知识文件的变更历史；但如果项目目录各自有 Git，岗位目录建仓时要把项目目录加进 `.gitignore`，避免嵌套仓库。

## 阶段 4：接线

目标：让助手在项目里干活时能读到上级知识。

1. 对每个纳入的项目，把 `templates/项目-AGENTS-追加段.md` 的内容追加到该项目 `AGENTS.md` 末尾。路径用相对路径（`../业务知识/`），项目在外部时用绝对路径。
2. 如果项目只有 `CLAUDE.md` 没有 `AGENTS.md`：追加到 `CLAUDE.md`。如果两者都有：追加到 `AGENTS.md`，并确认 `CLAUDE.md` 里有 `@AGENTS.md` 引用。
3. 向用户解释一句为什么要显式引用：Codex 和 dsh 只从 Git 根目录往下读规矩文件，岗位目录在 Git 根之上，不写这句它看不见。
4. 如果你是 WorkBuddy 或其他读不到会话记录的工具：追加段里要包含「会话末尾自记」那一节，让每次会话结束前把用户手打的业务判断原话追加到项目内 `_候选/本项目候选.md`。

## 阶段 5：首轮考古

目标：把历史会话里已经说过的判断先捞一遍。

1. **范围**：先只处理最近 90 天，或每个项目最多 300 条用户消息，取先到者。历史更久的告诉用户可以以后分批补。
2. **捞**：按 `tools/README.md` 的方法读会话记录，按项目路径过滤，只取用户消息。
3. **只保留手打的**，排除：
   - 长度超过 1500 字的
   - 含 2 个及以上 Markdown 标题的
   - 以「# 任务」「## 任务」开头的
   - 明显是粘贴进来的日志、报错、代码块
   - 工具自动注入的系统内容（例如插件推荐、环境说明）
4. **脱敏**：按通用护栏第 4 条处理。
5. **判读**：严格按 `prompts/weekly-distill.md` 的口径逐条判断，输出结构化结果。
6. **写入**：
   - 高置信 → 对应分类的正式文件，新增条目
   - 中低置信 → `_候选/观察池.md`
   - 每条都写「首次记录」日期，用会话发生日期而不是今天
   - `CHANGELOG.md` 追加一行：日期、来源项目、新增 N 条、入池 M 条
   - 生成 `_候选/weekly-<今天日期>/report.md`，用 `templates/weekly-report.md` 格式
7. **不要**在首轮就做「过期」「冲突」处理，没有历史可比。
8. 把报告给用户看，请他做四件事：宣布失效、裁决冲突（若有）、定优先级、补充机器读不到的背景。用户的回复按「用户裁决」写回条目。

## 阶段 6：建立每周例行

1. 给用户一条固定的每周提示词，让他每周一复制粘贴：
   ```
   按 ai-distill-kit 做本周蒸馏：读取最近 7 天本项目的会话记录，只取我手打的消息，按 业务知识/_候选/weekly-distill.md 判读，合并到 业务知识/，生成本周报告，然后把需要我决策的事项列出来。
   ```
2. 如果你所在的工具支持定时任务（Claude Code 的 scheduled tasks、系统 cron 调用 `codex exec`、dsh 的定时插件），询问用户是否设为每周一早上自动跑。用户同意再设，并把定时任务的名称和时间写进岗位 `AGENTS.md`。
3. 不支持定时的工具（例如 WorkBuddy），就把上面那条提示词写进岗位 `AGENTS.md` 的「每周例行」一节，靠用户手动触发。

## 阶段 7：汇报

用一段话告诉用户：

- 岗位目录在哪，纳入了哪些项目，哪些项目挪动过、旧路径是什么
- 首轮考古的漏斗数字：读了多少条 → 手打多少条 → 入正式多少条 → 入池多少条
- 每周例行怎么触发
- 他现在需要做的决策清单（来自阶段 5 的报告）
- 已知的局限：本工具读不到的会话、脱敏只覆盖凭据类、需要人工补的背景

汇报结束后停下，等用户决策。不要自作主张继续处理下一批历史。
